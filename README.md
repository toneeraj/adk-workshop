# my_agent

A small agent project. Contains basic agent code in `my_agent/`.

## Quick start

- Inspect the code in `my_agent/`.
- Create a virtual environment: `python3 -m venv venv`.
- Activate and install dependencies if any.